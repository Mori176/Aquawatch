name: my_fish_app
description: "IoT Aquaculture Water Quality Monitoring System - FYP UniKL MIIT"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.0
  firebase_database: ^11.3.10
  firebase_messaging: ^15.1.0
  cupertino_icons: ^1.0.8
  image_picker: ^1.2.3
  firebase_storage: ^12.4.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - images/AquaManager.png
